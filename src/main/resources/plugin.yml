name: ${project.artifactId}
main: ${project.groupId}.Main
version: ${project.version}
author: TrollyLoki

depend: [LuckPerms, FactCore]
softdepend: [Vault, SuperVanish, ClearLag, GriefPrevention, PlotSquared, WorldEdit, WorldGuard]

api-version: 1.15

commands:
  points:
  rtag-update:
    permission: factbukkit.update
  cancelclear:
  edit:
  stats:
  vote:
  servers:
  clearlag: